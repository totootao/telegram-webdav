# 大文件上传健壮性：分片重试 + 只补传失败分片

日期：2026-09-09
环境：`127.0.0.1:10010`（容器 `tg-webdav`，5 bot 池 + 自建 TG 代理）

## 问题

大文件（如 1.2GB = 60 个分片）上传时，**只要有一个分片失败就整体失败**返回 502，
客户端只能重传整个文件——那 59 片已成功的字节全白传，还会在频道里留下一堆孤儿消息。

根因是「任一分片失败即整体放弃」的失败模型。做个算术：设单分片最终失败率 1%，
60 个分片的整体成功率 = 0.99⁶⁰ ≈ **55%**，即近一半概率白传。分片越多越容易挂，
这正是大文件尤其容易失败的原因。

## 关于「能否只重传失败的那个分片」

**标准 WebDAV 下，客户端做不到。** PUT 是一次性请求，body 流式消费完就没了，
服务端无法让客户端「倒带」回第 N 片；也没有标准机制通知客户端重传某一片。
rclone / 资源管理器 等标准客户端只支持整文件 PUT。

**但服务端可以做到「实际只传失败的那一片」，用两条互补的路径：**

| 路径 | 机制 | 效果 |
|---|---|---|
| **A. 分片自动重试** | 某个分片失败时只重试该片（换 bot 槽位 + 退避），不牵连其他分片 | 整体上传直接成功，无需重传 |
| **B. 重传时复用已成功分片** | 按分片 SHA-256 去重；客户端重传整个文件时，已传过的片直接复用 file_id 跳过上传 | 客户端行为不变，实际只补传失败的那几片 |

## 实现

### A. 分片级重试（`webdav.py`）

`_upload_parallel` / `_upload_streaming` 中，把分片上传包成 `_one_retry`：
失败只重试**这一片**（`_UP_CHUNK_RETRY=3`），退避策略对 429 用 Telegram 返回的
`retry_after`，其他错误指数退避（上限 30s）。重试耗尽才整体失败。

### B. 分片去重复用（`db.py` + `webdav.py`）

新增 `chunk_dedup` 表（按分片 SHA-256 记录 `file_id / slot / message_id / size`）：
- 上传前先按分片 SHA 查表，命中则**直接复用原 file_id，跳过本次上传**
- 上传成功后写入记录
- 记录默认保留 30 天（`_CHUNK_DEDUP_TTL`），启动时清理过期项
- 开关：`TG_CHUNK_DEDUP`（默认 on）

file_id 与 bot 绑定，故连 `slot` 一起记录。这是**跨文件**的内容寻址复用
（不同文件中的相同数据块也能省一次上传）。

### 顺带修复的两个真实 bug

1. **`f2.cancel()` 在对 int 调用**（`_upload_parallel`）：
   `fut_map = {future: i}`，`values()` 是 int 不是 Future，`f2.cancel()` 必抛
   `AttributeError`，掩盖真实的 429/网络错误，502 变 500。已改为遍历 keys。

2. **`iter_chunk` 的 `total_yield` 未初始化**（本次重试改动引入，已修）：
   `total_yield = 0` 原本在 `try` 内部，若 `getFile`/`_do_get` 提前抛异常，
   except 里访问它会触发 `UnboundLocalError`，把真实错误变成 0 字节断流。
   已提到每次尝试前初始化。同时修正兜底逻辑不再吞掉 `_ClientGone`
   （客户端主动断开是正常信号，不该被误报成「下载分片失败」）。

## 验证结果

### 1. 去重：重传同一文件（100MB / 5 片）

| 次数 | 耗时 | 实际上传 |
|---|---|---|
| 第 1 次（全新） | 9.30s | 5 片全传 |
| 第 2 次（重传） | **0.14s** | **0 片**（5 片全部命中去重） |

提速 **66.3x**，下载 SHA-256 一致（复用 file_id 后内容正确）。

### 2. 只补传失败的分片（精准模拟）

模拟 5 片中第 2、4 片上次失败（删掉这两条去重记录），然后重传整个文件：

```
PUT 分片[0] 命中去重(跳过上传)
PUT 分片[1] 命中去重(跳过上传)
PUT 分片[3] 命中去重(跳过上传)
PUT 分片复用统计: 共 5 片，其中 3 片命中去重直接复用，2 片实际上传
```

→ 客户端仍是整文件 PUT，服务端**只真正上传了失败的那 2 片**，下载 SHA 一致。

### 3. 1.2GB 真实大文件

重传 1.2GB（60 片）时全部命中去重：**上传 1.29s / 927 MB/s、内存增量 4MB**
（对比真实上传 94.73s / 485MB）。完整测试 **14/14 通过**，包括此前必现失败的
`resume.seg1`。

> 注：该测试文件由 64MB 随机块循环生成，分片内容重复度高，故命中率 100%；
> 真实文件（内容不重复）重传时命中率取决于「上次成功了几片」——
> 失败 1 片则命中 59 片，只补传 1 片。

### 4. 分片重试单元测试（注入故障）

| 场景 | 结果 |
|---|---|
| 第2片前2次失败、第3次成功 | **整体上传成功**；调用次数 `{0:1,1:1,2:3,3:1,4:1}` —— 只重试失败那片 |
| 第3片永久失败 | 尝试 4 次后放弃，抛出 **TGError**（不再是 AttributeError） |

### 5. 回归

- `bigfile_test`（1.2GB）：**14/14**
- `real_test`（小文件 + 完整性/损坏检测）：**17/17**
- `prod_test`（真实环境）：**17/17**

## 配置

| 变量 | 默认 | 说明 |
|---|---|---|
| `TG_CHUNK_DEDUP` | on | 分片去重复用开关 |
| `_UP_CHUNK_RETRY` | 3 | 单分片失败重试次数（代码常量） |
| `_CHUNK_DEDUP_TTL` | 30 天 | 去重记录保留时长（代码常量） |

孤儿分片（上传最终失败时已传到频道的片）按约定**保留**，供后续重传复用，
不额外调用 deleteMessage（避免增加受限流约束的 API 调用）。
