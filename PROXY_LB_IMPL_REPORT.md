# 优选 IP 负载均衡：代码实现（默认关闭）

日期：2026-09-11　环境：广州腾讯云出口，真实 5 bot + 3 个 CF Pages 代理

---

## 一、这次做了什么

给代理候选加了一层「**域名 × IP**」的路由模型，以及一个**默认关闭**的选路开关。

```bash
# 默认（不写这两行）= 完全沿用「按序主备 + 失败切换」，行为零变化
-e TG_PROXY_LB=fastest \
-e 'TG_PROXY_IPS=tg-proxy-b4t.pages.dev=155.117.224.63;otterhub-tg-proxy-3uj.pages.dev=43.175.131.30;tg.totootao.top=209.33.166.199'
```

新增文件：

| 文件 | 作用 |
| --- | --- |
| `proxylb.py` | 选路 + 健康度记账；以及「TCP 连指定 IP、SNI 保持原域名」的连接原语 |
| `proxylb_test.py` | 30 项纯逻辑单测（不联网），覆盖 4 种模式 + 冷却 + 记账边界 |

改动的既有文件只有 `tg.py`：候选从 `(api_base, proxy_token)` 变成
`(api_base, proxy_token, ip)`，连接池 key 带上 `ip`，并在上传/下载处记账。
**第三项为 `None` 时所有代码路径与改动前逐字节一致。**

---

## 二、为什么是「域名 × IP」而不是「多域名轮询」

之前那份 [PROXY_LB_REPORT.md](PROXY_LB_REPORT.md) 的结论是「多代理负载均衡没有带宽收益，
不建议做」——那个结论**现在依然成立**，但它测的是「多个域名之间均分流量」。
真正的变量在 IP 上：同一批 CF 优选 IP，单车道吞吐差 20 倍
（23.2 / 6.2 / 2.6 / 1.1 MB/s），而域名只是 IP 的一个别名。

所以这次的实现**不是**把流量摊到多个代理上，而是：

1. 把每个域名展开成若干条「域名 × IP」路由；
2. 按实测吞吐挑**最快的那一条**；
3. 只在它挂了/变慢时才动。

`rr` 模式也做了，但**不推荐**——见第五节的反例。

---

## 三、真实环境 A/B（100MB 文件，同一台机器、同一时段）

| 模式 | 上传 | 下载 | 实际走的路由 |
| --- | --- | --- | --- |
| `off`（默认） | 6.63 s / **15.8 MB/s** | 10.58 s / 9.9 MB/s | 全部 DNS 解析 |
| `fastest` | 3.62 s / **29.0 MB/s** | 9.99 s / 10.5 MB/s | 全部 `43.175.131.30` |

- 上传 **+83%**；下载基本持平（下载瓶颈在别处，见 [CF_PREFERRED_IP_REPORT.md](CF_PREFERRED_IP_REPORT.md)）。
- 两次都是 `201 / 200`，下载字节 MD5 与源文件一致。

启动/运行日志里可以直接核对：

```
[tg] proxy_lb=fastest ips=3 penalty=0.5 explore=0.1 cooldown=60.0s keep_dns=on min_bytes=262144
[tg] 代理路由统计: lb[fastest] otterhub-...@43.175.131.30:tp=6.3MB/s ok=4 fail=0
                              | tg.totootao.top@209.33.166.199:tp=2.2MB/s ok=3 fail=0
```

---

## 四、四种模式怎么选

| 模式 | 做法 | 适用 |
| --- | --- | --- |
| `off` | 按序主备 + 失败切换（**默认**） | 不确定就用这个 |
| `fastest` | EWMA 吞吐择优 + 在途惩罚（并发自动铺开到次快车道） | **推荐** |
| `weighted` | 按吞吐加权随机 | 多车道并发、要平滑分摊 |
| `rr` | 严格轮询 | 仅当各 IP 速度接近 |

「在途惩罚」这条是特意加的：`score = 吞吐 / (1 + 在途数 × 0.5)`。
因为实测单车道有天花板（约 57~62 MB/s，链路级不是 Worker 级），
把所有请求堆到"最快"那一条反而更慢；加了惩罚后并发会自动铺开。

---

## 五、实现过程中发现并修掉的问题

这些都不是纸面推演，是跑出来的：

1. **冷启动会把自己锁死。** 最初写的是「没有样本就按配置顺序」——结果第一条永远
   被选中、其余路由永远拿不到样本，择优**永久退化**成「只走配置里第一条」。
   修法：`fastest` 用 ε-greedy 探索（默认 10%），`weighted` 冷启动等概率随机，
   没测过的路由给「等于当前均值」的乐观先验而不是 0 权重。
   代价实测可见：把 `TG_PROXY_LB_EXPLORE` 从 0.1 提到 0.6 后，10 次 20MB 上传的
   平均耗时从约 3.5 s 涨到约 7 s——**探索不是免费的**，所以默认只给 10%。

2. **小请求会打死好路由的 EWMA。** `getFile` 只有几百字节，一次 RTT 抖动就能算出
   「几百 B/s」，足以让一条快路由被永久降权。修法：小于 `TG_PROXY_LB_MIN_BYTES`
   （默认 256KB）的请求只更新延迟、不更新吞吐。

3. **429 / 4xx 不该算在路由头上。** 它们是 bot 级或请求级问题，换 IP 没用。
   计入健康度只会误杀好路由 → 标成 `neutral`，只减在途、不计失败。

4. **坏 IP 会刷爆日志。** 连接预热线程每 2s 给每个路由握手一次，一个坏 IP 就是
   每 2s 一条错误日志。修法：同一路由 60s 内最多打一条，且正在冷却的路由跳过预热。
   （全坏 IP 场景下日志从几百条降到 12 条。）

5. **`rr` 在速度不均时是有害的**，实测反例：6 次 20MB 上传里，轮到 `209.33.166.199`
   的那次用了 **70.7 s**，其余 5 次都在 3~8 s。这正是旧报告「不建议做负载均衡」的
   那个坑，所以 rr 只留作「几个 IP 速度确实差不多」时的选项。

6. **`TG_PROXY_LB_KEEP_DNS` 默认保留 DNS 兜底路由**（默认 `on`）。关掉它、且所有
   优选 IP 同时失效时，单次上传最坏要 190 s 才返回 502——因为候选数变多，
   「每个候选 3 次重试 × 5 个 bot」的耗时被放大。默认开着就没有这个问题。

---

## 六、回归测试

| 项目 | 结果 |
| --- | --- |
| `proxylb_test.py`（新增，30 项） | 全通过 |
| `proxy_failover_test.py` A1~A9（含新增的 A8/A9：默认 off 不展开 IP、开启后正确展开） | 9/9 |
| `download_stop_test.py` | 20/20 |
| `retry_unit_test.py` | 通过 |
| `selftest.py` | 70/71（唯一失败项 `put.name.multi.fname` 在改动前就存在，与本次无关） |
| 真实环境 `off` 模式：PUT 50MB / GET / Range / MKCOL / PROPFIND / 401 / DELETE | 201 / 200（MD5 一致）/ 206（MD5 一致）/ 201 / 207 / 401 / 204 全通过 |
| 真实环境 `fastest` / `weighted` / `rr` | 上传下载均 201/200，下载 MD5 一致 |

---

## 七、建议怎么用

1. **先按现状跑着**（`TG_PROXY_LB` 不设），确认功能没问题。
2. 想提速再开：先给**一个**域名配**一个**实测最快的 IP，`TG_PROXY_LB=fastest`。
3. 跑几个大文件后看 `代理路由统计:`——如果那条 IP 的 `tp` 明显高于其他，就说明生效了。
4. 有多个都很快的 IP 时，可以给同一个域名配多个 IP
   （`域名=ip1,ip2`），让在途惩罚把并发铺开。
5. **`rr` 别乱用**，除非你确认几个 IP 速度接近。

> 注意：`TG_PROXY_IPS` 里写死的 IP 会过期。CF 边缘 IP 变动或当前 IP 被限速后，
> 吞吐会掉下来——这时 LB 会自动把流量挪到别的路由（包括 DNS 兜底），
> 但要长期保持最优，还是得定期重测优选 IP。
