# 多个文件下载：给 TG 代理做负载均衡，可行吗？

结论先行：**技术上完全可行，但实测没有带宽收益，当前不建议做。**
真正该做的是「**延迟感知的主备切换**」，而不是「流量均分」。

> **2026-09-11 补充**：本文测的是「**多个域名之间**均分流量」，结论不变。
> 但后来发现真正的变量在 IP 而不在域名（同一批 CF 优选 IP 单车道吞吐能差 20 倍），
> 于是实现了「域名 × IP」的选路，**默认关闭**，见
> [PROXY_LB_IMPL_REPORT.md](PROXY_LB_IMPL_REPORT.md)。
> 其中 `rr`（严格轮询）模式实测确认了本文的判断：速度不均时它会把请求均分到慢 IP，
> 单个 20MB 分片从 3s 涨到 70s。所以那个模式只在各 IP 速度接近时才该用。

- 测试对象：3 个自建 CF Worker 代理（`otterhub` / `b4t` / `tg.totootao.top`）、20MB 单分片文件、真实 Telegram
- 方法：`curl` 直连代理做并发阶梯，绕开服务端，只看代理本身的叠加能力

---

## 一、先确认现状：多文件下载是不是都打在同一个代理上

是的。用服务端并发下载 3 个文件，日志里 `proxy=` 全部是首选代理：

```
3 proxy=https://otterhub-tg-proxy-3uj.pages.dev/tg
```

（顺带排掉一个误判：并发确实是并发的——3 个请求同一秒开始、同一秒结束，每路 6.7~7.9 MB/s，
聚合约 22 MB/s。之前一次测到的 7.76 MB/s 是**时段性网络拥塞**，不是服务端串行。）

---

## 二、关键实验：单代理到底能撑多少路

| 场景 | 路数 | 聚合吞吐 | 每路 |
|---|---|---|---|
| 单代理(otterhub) | 1 | 7.03 MB/s | 7.03 |
| 单代理 | 2 | 15.11 MB/s | 7.56 |
| 单代理 | 4 | 27.27 MB/s | 6.82 |
| 单代理 | 6 | 36.86 MB/s | 6.14 |
| 单代理 | 8 | 44.58 MB/s | 5.57 |
| **单代理** | **12** | **72.65 / 65.80 MB/s** | 6.05 / 5.48 |
| 两代理各 3 路 | 6 | 32.25 MB/s | 5.37 |
| 两代理各 6 路 | 12 | 64.81 MB/s | 5.40 |
| 三代理各 2 路 | 6 | 40.25 MB/s | 6.71 |
| 三代理各 3 路 | 9 | 41.36 MB/s | 4.60 |

**读出来的三件事：**

1. **单代理远未饱和**：1→12 路，聚合吞吐几乎线性（7 → 66~73 MB/s），每路只从 7.03 掉到 5.5（-22%）。
   也就是说单代理的带宽天花板在 **70 MB/s 以上**，日常几个到十几个并发根本碰不到。
2. **同路数下，分流反而略慢**：12 路时单代理 65.8~72.7 MB/s，两代理均分只有 64.81 MB/s。
   原因很简单——次选代理更慢，把流量分给它等于拉低平均。
3. **真正的限制是「单条 TCP 连接」约 6~7 MB/s**（跨境单流速率），不是代理带宽。
   想快，加**并发路数**；换个代理分发同样的路数，不会更快。

---

## 三、代理之间的质量差多少

| 代理 | 单路吞吐 | 备注 |
|---|---|---|
| otterhub-tg-proxy-3uj | **9.27 MB/s** | 当前首选，最快 |
| tg-proxy-b4t | 7.66 MB/s | 次选 |
| tg.totootao.top | 6.70 MB/s | 测试中曾出现一次 **56 B/s** 的临时故障 |

最后一行是重点：**代理会「半死不活」**——不报错，只是极慢。如果做均分轮询，
就会持续把 1/3 的流量送给这种慢代理，体感直接变差。

---

## 四、那负载均衡什么时候才有意义

不是带宽，而是这两件事：

1. **配额 / 请求数**：CF Worker 免费版按天计请求数。20MB 分片下，单日 2TB 下载 ≈ 10 万次请求，
   单代理就接近天花板了。多代理可以把**请求数配额**摊开——这才是最实在的收益。
2. **单点故障**：首选代理整体挂掉时，流量能否自动走别的（现在的逻辑已经支持：失败才切）。

---

## 五、建议：做「延迟感知的主备」，别做「均分」

按优先级：

| 优先级 | 动作 | 收益 |
|---|---|---|
| P0 | **代理健康/延迟感知**：记录每个候选的首字节与吞吐 EWMA，首选变慢（不是失败）也能临时切到次选；连续失败则摘除，冷却后探活恢复 | 解决「半死不活代理拖慢一切」，比均分有用得多 |
| P1 | 保持并发路数不受限（当前 ThreadingHTTPServer 天然多线程，已验证可并发） | 每多一路 ≈ +6 MB/s |
| P2 | 真要分流时，按**实测权重**（约 9.3 : 7.7 : 6.7）分配，而不是轮询均分；且默认关闭 | 只在配额吃紧时开启 |

如果确实要上分流，建议的最小改动方案（约 30 行 + 一个开关）：

- **按 slot 轮转首选代理**：`slot i → 候选[i % N]`。分片上传本来就按 slot 轮转分摊到 5 个 bot，
  这样不同分片天然落在不同的首选代理上，后续候选仍作为故障切换保留——不动现有失败语义，风险最低；
- 加权重（EWMA 吞吐）+ 健康摘除；
- 开关 `TG_PROXY_LB=off|slot|weighted`，默认 `off`（当前无收益，默认关闭最稳）。

---

## 六、一句话回答

> 多文件下载时给 TG 代理做负载均衡**可行但没必要**：实测单代理 12 路能跑 66~73 MB/s 且远未饱和，
> 同路数分流到多代理反而略慢（慢代理拖后腿）。瓶颈是单连接 ~6~7 MB/s 的跨境速率，不是代理带宽。
> 想提速就**加并发路数**；想稳就做**延迟感知的主备切换 + 健康剔除**；
> 只有当日请求数配额成为瓶颈时，才需要真正的分流（且要按权重分，不要均分）。
