# 视频下载速度实测报告

> 对象：`/prodtest/big.bin`（60MB / 3×20MB 分片，等效 60MB 视频）
> 链路：WebDAV → 自建代理 `tg.totootao.top/tg` → Telegram
> 时间：2026-09-09

## 1. 一句话结论

**健康状态下 13~16 MB/s（103~132 Mbps），分片边界零卡顿，播放流畅。**
但今天连续压测累计下载 **1.26 GB 之后，速度掉到 5.5~6.4 MB/s，且冷却 4 分钟不恢复** ——
瓶颈在**代理 / Telegram 侧**，本项目无责，也没有可调的旋钮能救。

## 2. 峰值（健康状态，今天早些时候）

| 场景 | 平均速度 | TTFB | 最大块间隔 | p95 | 卡顿 >300ms |
| --- | --- | --- | --- | --- | --- |
| 全量下载（无 Range） | **12.93 MB/s**（103.5 Mbps） | 1461ms | 553ms | 49ms | 2 次 |
| 播放式（Range: bytes=0-） | **16.56 MB/s**（132.5 Mbps） | 549ms | 771ms | 61ms | 2 次 |
| seek 30% | 5.93 MB/s | 345ms | 340ms | 226ms | 1 次 |

服务端分片级吞吐：**14.50 / 14.68 MB/s**，分片间隔 **0.000s**（跨分片零停顿）。

逐秒速度曲线（全量）：

```
 1s   1.25MB/s  █                    ← 前 1s 在付 getFile + 首字节
 2s  18.75MB/s  ██████████████████
 3s  20.00MB/s  ████████████████████
 4s  20.00MB/s  ████████████████████
```

## 3. 降速后（累计 1.26GB 之后）

| 测量方式 | 结果 |
| --- | --- |
| WebDAV + curl，3 次 | 6.40 / 5.81 / **5.54 MB/s** |
| 冷却 4 分钟后再测 3 次 | 6.40 / 5.81 / **5.54 MB/s**（**不恢复**） |
| **直连代理（完全绕开 WebDAV）** | **5.90 → 2.02 MB/s** |
| 播放式 3 轮 | 5.24 → 4.24 → **3.12 MB/s**（逐轮递减） |

单次传输内部也是前快后慢（典型流量整形特征）：

```
 0s  2.50MB/s   5s   1.75MB/s   █      10s  1.75MB/s
 1s 10.00MB/s   6s   2.00MB/s          15s  1.75MB/s
 2s  7.50MB/s   7s   1.50MB/s          18s  0.50MB/s
 4s 10.50MB/s   8s   1.50MB/s          19s  0.50MB/s
```

**播放体验同步恶化**：p95 从 49ms 涨到 **312~425ms**，卡顿 >300ms 从 2 次涨到 **14~23 次**。
也就是说一旦被限速，不只是下载慢，**播放会开始一卡一卡的**。

## 4. 因果判定：不是项目的问题

三条例证：

1. **直连代理也慢**（5.90 → 2.02 MB/s）—— 完全绕开 WebDAV，走同样的代理拉同一个 20MB 分片，
   速度一样烂。**这是决定性的对照实验。**
2. **服务端无任何错误** —— 日志里没有 429、没有限流、没有代理切换、没有重试失败。
   分片级吞吐如实记录为 5~9 MB/s，说明服务端在老实地转发，只是上游给得慢。
3. **`getFile` 全部命中缓存**（0ms）—— 所以慢的也不是元数据查询那一环。

结论：上游（自建代理 或 Telegram）在累计一定流量后做了限速/配额控制。

## 5. 还能做什么

| 方向 | 做法 | 评价 |
| --- | --- | --- |
| **加代理候选做分摊** | 项目已支持多代理池（`proxy_pools`），当前**每个槽位只有 1 个候选**。多配几个 TG 代理，限速时自动切换/轮询分摊 | ⭐ 最现实 |
| 查代理配额 | 若代理跑在 Cloudflare Worker 上，免费版有请求数/CPU 时间/带宽配额，控制台能看到是否触顶 | ⭐ 先确认病因 |
| 项目侧调参 | **没有可用旋钮**：分片大小已卡在 Telegram 20MB 上限，单/多线程下载都试过（见下） | ❌ 无用 |
| 加并发 | 实测无效，见下 | ❌ 已证伪 |

### 并发阶梯（并发能不能提速？）

| 路数 | 聚合吞吐 |
| --- | --- |
| 1 路 | 13.64 MB/s |
| 2 路 | 11.54 MB/s |
| 4 路 | 14.44 MB/s |

**并发完全不提速** —— 1 路和 4 路一样。这印证了项目里"单文件固定单线程串行下载"的设计是对的：
多开线程只会互相抢连接池、更容易触发上游限流，拿不到额外带宽。播放器多线程预取在这里没用。

## 5b. 多 IP 分摊：可行但收益有限（2026-09-09 补充）

代理域名 `tg.totootao.top` 解析到 2 个 IP，都在 Cloudflare 段（`172.66.44.220` / `172.66.47.36`）。
做了「直连指定 IP + SNI/Host 仍用域名」的实验（Host 头必须保留域名，否则 Cloudflare 不路由）：

| 方式 | 速度 |
| --- | --- |
| 单 IP `172.66.44.220` | 3.76 MB/s |
| 单 IP `172.66.47.36` | 4.57 MB/s |
| 两 IP 并发 | 5.41 / 7.03 MB/s |

**提升约 35~54%，低于理论叠加（8.33）** —— 两个 IP 各有带宽，但共享一个总天花板
（Cloudflare 背后是同一套后端/配额）。

### 一个容易忽略的事实：现在连「多文件并发」也用不上第二个 IP

```
容器内建 6 次连接，实际连到的 IP 分布：
   172.66.47.36: 6 次          ← 第二个 IP 从未被使用
```

Python 的 `socket.create_connection` 会按 `getaddrinfo` 顺序遍历，**第一个能连上就用它**，
不会在多个 IP 之间分摊。所以：

- 单文件串行下载：同时只有 1 条连接 → 多 IP 无意义 ✅
- **多文件并发下载：所有连接也全挤在同一个 IP** → 多 IP 同样浪费 ❌

> 注：`create_connection` 本身已具备**故障切换**（前一个 IP 连不上会试下一个），
> 所以多 IP 目前提供的只是容灾，不是提速。

### 若要做，改动很小（约 20 行）

在 `tg.py` 的 `_open_conn` 层：启动/定时解析出全部 IP → 轮转选一个 →
`socket.create_connection(ip)` + `wrap_socket(server_hostname=域名)`，
并在 `_do_get` 里**显式带上 `Host: 域名`**（否则 Cloudflare 按 IP 拒绝，实测会拿到 0 字节）。
连接池 key 也要把 IP 纳入，否则复用会串。

## 6. 复现

```bash
python3.11 speed_test.py 3      # 每场景 3 轮取中位数，含 seek 与并发阶梯
```

> 注意：跑一次会消耗几百 MB 流量，**会加速触发上游限速**。想拿到干净的峰值数据，
> 请在代理配额重置之后再跑。
