# file_path 预热报告（首字节加速 · 不落库版）

- 环境：真实 Telegram bot 池（5 个 bot）+ 自建代理 `https://tg.totootao.top/tg`
- 时间：2026-09-09
- 结论：**首片 TTFB 1.803s → 1.408s（PROPFIND 预热）/ 1.196s（HEAD 预热）**，首片 getFile 未命中率 3/3 → 0/3
- 回归：selftest 71/71、real_test 17/17、prod_test 17/17（真实 Telegram）全通过

---

## 一、为什么不做「上传时把 file_path 落库」

上一轮提出过落库方案（下载时彻底省掉 getFile），**最终否决**，原因是硬伤：

> Telegram 的 `file_path` **有效期只有 1 小时**。写进数据库，隔一段时间再读就是过期路径，
> 下载会直接失败——用一个偶发故障换 1s 的提速，不划算。

所以改为**内存缓存（TTL 50 分钟）+ 提前预热**：不碰数据库，只让 getFile **提前发生**在用户不需要等待的时刻。

---

## 二、先量清楚：getFile 到底慢在哪

用 3 个真实 file_id 直连代理实测（绕过服务端缓存）：

| 测量项 | 结果 |
| --- | --- |
| 单次 getFile | **1.076s / 1.013s / 1.057s** |
| 3 路并发 getFile | **总计 0.758s**（各 0.725 / 0.724 / 0.756） |
| 同一 file_id 连取 2 次 | 0.695s / 0.728s（无服务端缓存） |

两个关键结论：

1. **getFile ≈ 1s，且并发不排队**（3 路并发只要 0.758s，比串行 3.1s 快 4 倍）— 这正是上一轮「并发预取」有效的原因。
2. 剩下的就是**纯 RTT**，无法通过并发再压缩，只能靠**提前做**。

### 首片 TTFB 的构成（40MB / 2 分片，冷缓存）

```
GET 请求进入
 ├─ getFile（首片）            ≈ 1.0s   ← 可预热消除
 ├─ Telegram Range 首字节      ≈ 0.9s   ← 网络 RTT，无法消除
 └─ 首字节到达客户端          总计 1.9s
  （同时后台并发预取第 2 片 getFile，0.704s，藏在首片下载里）
```

热缓存（省掉 getFile）后：40MB 文件 TTFB 降到 ~1.1~1.4s，小文件（2KB）降到 0.18~0.54s。

---

## 三、方案：在用户不等待的时候做 getFile

| 时机 | 行为 | 覆盖场景 |
| --- | --- | --- |
| **PROPFIND 列目录** | 后台预取目录内各文件的**首片** file_path | 客户端列目录 → 点开文件（绝大多数场景） |
| **HEAD 探测** | 后台预取该文件的**全部分片** | 播放器 / 下载器探测后紧跟 GET |

设计要点：

- 预热在响应**发出之后**由 daemon 线程执行，**不阻塞**列目录（实测 PROPFIND 仍 **2.5ms** 返回）
- 走独立连接（`_get_file_path_direct`），不与下载争用 keep-alive 连接池
- 已缓存的分片自动跳过，重复列目录零开销
- 同时最多 2 个预热任务（`_WARM_MAX_ACTIVE`），避免大目录反复列目录打爆代理
- 任何失败静默忽略，下载时自动回退到常规 getFile

新增配置项：

| 变量 | 说明 | 默认 |
| --- | --- | --- |
| `TG_WARMUP_PROPFIND` | 列目录时预热各文件首片 | `on` |
| `TG_WARMUP_HEAD` | HEAD 探测时预热全部分片 | `on` |
| `TG_WARMUP_MAX_FILES` | 单次列目录最多预热文件数 | `20` |

---

## 四、A/B 实测（每场景 3 轮，重启清缓存，取中位数）

代理 RTT 有波动（首片 TTFB 单轮在 1.7~2.1s 间摆动），所以每场景跑 3 轮取中位数。

### 大文件 40MB（2 分片，播放/下载场景）

| 场景 | 首片 TTFB（各轮） | 中位 | 相对基线 | 首片未命中 getFile |
| --- | --- | --- | --- | --- |
| S1 冷启动直接 GET（基线） | 1.803 / 1.709 / 2.068 | **1.803s** | — | **3/3 轮** |
| S2 PROPFIND 列目录 → 等 2s → GET | 1.414 / 1.294 / 1.408 | **1.408s** | **-21.9%** | **0/3 轮** |
| S3 HEAD 探测 → 等 1s → GET | 1.196 / 1.385 / 1.076 | **1.196s** | **-33.7%** | **0/3 轮** |

全量下载总耗时中位数：3.931s → 3.496s → 3.109s。

### 小文件 2KB（单片）

| 场景 | 首片 TTFB | 相对基线 |
| --- | --- | --- |
| 冷启动直接 GET | 1.012s | — |
| PROPFIND 预热后 GET | **0.543s** | **-46.3%** |
| HEAD 预热后 GET | 0.586s | -42.1% |

> 小文件改善幅度更大：它本来就只有一次 getFile，预热直接把这部分全砍掉了。

---

## 五、配置开关验证

| 验证项 | 期望 | 实测 |
| --- | --- | --- |
| `TG_WARMUP_MAX_FILES=2`（目录 4 个文件） | 只预热 2 个 | `文件数=2` ✅ |
| `TG_WARMUP_PROPFIND=off` | 完全不预热 | 预热触发 0 次 ✅ |
| 默认配置 | 预热 4 个 | `文件数=4` ✅ |
| PROPFIND 是否被预热阻塞 | 否 | 响应 **2.5ms** ✅ |
| 预热是否真取到 file_path | 是 | `getFile 预取成功 ... 耗时=0.737s` ✅ |

---

## 六、回归测试

| 测试 | 结果 |
| --- | --- |
| `selftest.py`（本地假 Telegram，单元+端到端） | **71/71** |
| `real_test.py`（模拟慢速网络，分片平滑度） | **17/17** |
| `prod_test.py`（真实 bot 池 + 自建代理 + 真实频道） | **17/17** |

---

## 七、剩余瓶颈与可选方向

首片 TTFB 现在仍有 ~1.2~1.4s，构成是：

- Telegram Range 首字节 ~0.9s（**纯网络 RTT，服务端无法优化**）
- 代理转发开销（可选优化点：**代理侧缓存 getFile 结果**，能同时省掉预热的请求量）

如果还想再压，优先级建议：

1. **代理侧优化 getFile**（目前 ~1s 偏高，正常 Telegram API 直连通常 200~400ms）——收益最大，且对所有场景生效
2. **预热范围扩大到子目录**（当前只预热当前层；`Depth: infinity` 客户端可覆盖）
3. 若要彻底省掉 getFile，只能违反「不落库」前提——**不建议**

---

## 八、复现

```bash
python3 warmup_test.py          # 小文件 + 大文件，每场景 3 轮取中位数
SKIP_SMALL=1 python3 warmup_test.py   # 只跑大文件（40MB）
```

改动文件：`webdav.py`（预热逻辑与接入点）、`config.py`（3 个配置项）、`README.md`、本报告与 `warmup_test.py`。
