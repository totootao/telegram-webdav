# seek 中段 TTFB 偏高：定位与优化报告

- 环境：容器 `tgwd-seek`（5 bot 池 + 3 代理候选 + 并发 5/5），真实 Telegram + 自建 CF Worker 代理
- 样本：60MB 文件（3×20MB 分片）、20MB 单分片直连代理基线
- 时间：2026-09-10

---

## 一、先把钱花在哪看清楚（直连代理基线）

绕开 WebDAV 服务端，直接用 curl 打代理（同一个 20MB 分片，两轮一致）：

| 场景 | 首字节 | 总耗时 | 吞吐 |
|---|---|---|---|
| 冷连接 + 整片 20MB | 0.70 / 0.78s | 2.23 / 2.46s | 9.4 / 8.5 MB/s |
| **热连接 + 整片 20MB** | **0.50 / 0.49s** | 2.16 / 2.40s | 9.7 / 8.7 MB/s |
| 冷连接 + 片内 10MB | 0.72 / 0.74s | 2.06 / 2.14s | 5.1 / 4.9 MB/s |
| **热连接 + 片内 10MB** | **0.36 / 0.35s** | 1.84 / 1.68s | 5.7 / 6.2 MB/s |

三个结论：

1. **TLS 握手占 0.33~0.38s**（`time_appconnect`），热连接可以直接省掉这一整段；
2. 代理回源 Telegram 的首字节还要 **0.35~0.8s**，这是网络固有成本，服务端消不掉；
3. 「片内 Range」（seek 到分片中间）吞吐只有整片的一半（5~6 vs 8.5~9.7 MB/s），但**首字节反而略快**——所以 seek 慢不是因为「偏移」，而是因为**连接是冷的**。

服务端内部视角也对得上（同一次请求、同一代理、同一时刻）：

```
分片[1] range=10485760-20971519  首字节=1.377s  吞吐=4.41 MB/s   ← 首片，冷连接
分片[2] range=0-20971519         首字节=0.342s  吞吐=20.11 MB/s  ← 复用上面的连接
```

---

## 二、为什么 seek 之后连接总是冷的

播放器 seek 后通常只读几 MB 就断开或重新 seek。HTTP/1.1 下 **body 没读完的连接不能复用**，只能丢弃（`iter_chunk` 的 `GeneratorExit` 分支）。于是每次 seek 都要重新握手 —— 这正是 1.0~1.5s 里最容易拿掉的那 0.35s。

---

## 三、试过并且**否决**的两个方案（数据说话）

### 方案 A：把大 Range 切成小段（2MB + 8MB…）——有害，已回退

想法很自然：小段能读完 → 连接能回池。实测**反面**：

| | 60MB 全量 | 播放式 | 1 路并发 |
|---|---|---|---|
| 不切分（旧） | 20.07 MB/s | 23.87 MB/s | 22.87 MB/s |
| 切分（2MB+8MB） | **7.69 MB/s** | **6.16 MB/s** | **10.80 MB/s** |

原因：每多切一段就多付一次首字节成本，2MB 段的 0.35s 首字节比它 0.2s 的传输还久，固定 RTT 摊不薄反而被放大。

> 代码保留、默认关闭：`TG_SPAN_MB=0`（默认）。换环境（代理 RTT 极低但长连接被限速）可以一键验证。

### 方案 B：首片整片下载 + 丢弃前缀 ——更慢

想绕开「片内 Range 吞吐低」。算账：整片 20MB 热连接要 2.16s，而片内 10MB 只要 1.84s；多下一倍数据换不来更快。**不成立**。

---

## 四、采纳并落地的方案：连接预热（保活）

`tg.py` 新增后台守护线程：让每个代理 host 的池里**常驻一条只做过 TCP+TLS 握手的热连接**，
请求一来直接发，不用现握手。

配套三处：

1. **连接池 idle TTL 30s → 120s**（`TG_CONN_IDLE_SEC`）：播放器暂停/拖进度条常间隔几十秒，30s 一过就白付一次握手。放长后的保险是「取出前探活」+「请求失败即清空该 host 整池」。
2. **请求失败即清池**（新增 `_drop_pool`）：一次失败说明该 host 的连接多半已被中间盒静默 FIN，不清会连续拿到僵尸连接。
3. **探活语义收紧**：旧逻辑是「PEEK 到 EOF 才丢」，会把「有残留数据可读」误判成健康——
   空闲的 keep-alive 连接**本不该有任何可读字节**，有就说明上一个响应的 body 没读完，
   复用它会让下一个请求读到上一段的尾巴（长度还对、SHA 却不对，最难查的那种错位）。
   现在只要 `select` 报可读就整条丢弃。

开关：`TG_CONN_WARM=0` 关闭；`TG_CONN_WARM_INTERVAL`（巡检间隔，默认 2s）、`TG_CONN_WARM_IDLE`（预热连接空闲多久重建，默认 25s）。5 分钟无请求自动停止预热，不空转骚扰代理。

### 效果（seek 到 6 个位置 × 3 轮，取中位数，只读 1MB 即断开）

| 位置 | 关闭预热 | 开启预热(巡检2s) |
|---|---|---|
| 片0起点(0MB) | 1472ms | 1088ms |
| 片0中间(10MB) | 1147ms | 837ms |
| 片1起点(20MB) | 1132ms | 765ms |
| 片1中间(30MB) | 1229ms | 1071ms |
| 片2起点(40MB) | 1464ms | 802ms |
| 片2中间(50MB) | 1132ms | 1046ms |
| **平均** | **1263ms** | **935ms（-26%）** |

最好情况从 1043ms 降到 **741ms**。另有一组对照（预热关 + idle 30s = 原行为）平均 1322ms，与 1263ms 一致，说明收益来自预热本身而非时段波动。

---

## 五、还剩多少空间 / 还能做什么

seek 首块 ≈ 0.35s（握手，已优化掉）+ 0.35~0.8s（代理+TG 固有首字节）+ 抖动。剩下能动的：

| 方向 | 预期收益 | 代价 | 建议 |
|---|---|---|---|
| **代理延迟感知/首字节赛马** | 0.3~0.7s | 赛马要多发一份请求（可及时取消） | 三个代理实测首字节 0.68 / 0.75 / 1.40s，**差异接近 2 倍**。当前固定用候选 0（恰好最快）；若首候选不是最快的，收益很大。建议先做「EWMA 延迟排序」，零带宽成本 |
| getFile 预取 | 0（已缓存命中） | — | `file_path` 缓存 50min，实测 seek 全部命中，无需再优化 |
| 减小分片（如 10MB） | 负面 | 更多分片=更多消息 | 不推荐 |
| 换更快代理 / 多线接入 | 取决于代理质量 | 运维成本 | 这是真正的天花板 |

---

## 六、回归

| 套件 | 结果 |
|---|---|
| `download_stop_test.py` | **20/20**（新增 5 条：清池、分段开关与切分正确性） |
| `timeout_log_test.py` | **18/18** |
| `proxy_failover_test.py` | **7/7** |
| `full_real_test.py` | **76/76** |
| `edge_real_test.py` | **26/26** |

> 期间一次 `edge_real_test` 报 R14/R15 失败，排查为**我同时跑了两个实例**共用 `/edgetest` 目录互相删除（服务端日志可见第二次 PUT 覆盖），单实例重跑 26/26，与代码无关。

改动文件：`tg.py`（预热线程 / idle TTL 可配 / 失败清池 / 分段能力默认关闭）、`download_stop_test.py`（用例）、新增 `seek_diag.py`（诊断工具）与本报告。
